import React from 'react';

const SideBar = React.memo(() => {
    return (
        <div className="bg-white h-screen fixed" style={{width:'320px'}}>
            salam
        </div>
    )
})
export default SideBar;